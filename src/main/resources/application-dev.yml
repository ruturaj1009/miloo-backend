spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.miloo: DEBUG
    org.springframework.security: INFO
    org.springframework.web.socket: DEBUG
    org.hibernate.SQL: DEBUG
